# 项目交接文档

> ⚠️ 任何新会话/新 AI 接手前必读此文档。**每次会话结束必须更新本文件。**
> 最后更新：2026-08-14 · 当前阶段：**Sprint 2 多实例编排验证通过！drill run --all 2实例42秒全过**

---

## 1. 项目一句话
MySQL 自动化备份与恢复演练工具。管理机通过 SSH 远程控制恢复机：**用 Docker 容器隔离多版本 MySQL**，从备份源机拉取 xtrabackup 备份，依次恢复 + 验证 + 出审计报告。Python 3.11，SQLite 元数据库。

## 2. 当前状态
- 整体进度：🟢 **ADR 全锁定 + 设计文档齐全 + Sprint 1 核心代码完成**
- 最后可运行版本：Sprint 1 核心（`pytest tests/unit` 87 测试全过，`toolkit` CLI 可用）
- 健康度：✅ 无阻塞，可直接联调测试机
- **架构决策：ADR-01~10 全部锁定**（见第 4 节）
- 🆕 **Sprint 1 核心完成（87 单元测试全过）**：
  - ✅ T1 `core/executor.py` —— LocalExecutor + SSHExecutor + FakeExecutor（16测试）
  - ✅ T2 `core/config.py` —— pydantic 配置加载 + 环境变量插值（14测试）
  - ✅ T3 `core/logger.py` —— RotatingFileHandler + 幂等配置（4测试）
  - ✅ T4 `core/models.py` + `core/db.py` —— SQLAlchemy 6表ORM + 会话管理（9测试 + TaskStatus/version 3测试）
  - ✅ T5 `installer/docker.py` —— DockerInstaller 常驻容器池管理（18测试）
  - ✅ T6 `backup/locator.py` + `backup/xtrabackup.py` —— 备份定位 + xtrabackup封装（10测试）
  - ✅ T7 `recovery/verifier.py` —— ADR-10 自动发现验证（7测试）
  - ✅ T8 `recovery/task.py` —— ADR-09 12步恢复流程（5测试）
  - ✅ T9 `cli.py` + `__main__.py` —— CLI 命令接入（mysql/backup/drill/config 4组命令，dry-run 模式验证通过）

## 3. 已完成
| 产物 | 文件 | 状态 |
|---|---|---|
| 立项书 | docs/00-立项书.md | ✅ |
| PRD（9 功能点，FP-01/02/03/04 已适配 ADR-08/09/10） | docs/01-PRD.md | ✅ |
| 架构设计（ADR 全锁定 + 执行抽象层） | docs/02-架构设计.md | ✅ |
| 数据库设计（6 表，兼容 MySQL，附迁移指南） | docs/03-数据库设计.md | ✅ |
| 安全设计 | docs/04-安全设计.md | ✅ |
| 测试策略 | docs/05-测试策略.md | ✅ |
| AI 协作开发指南 | docs/06-AI协作开发指南.md | ✅ |
| 技术调研报告（pyxtrabackup + restore-drill） | docs/07-技术调研报告.md | ✅ |
| CLI 命令设计 | docs/08-CLI设计.md | ✅ |
| 企业微信通知模板 | docs/09-通知模板设计.md | ✅ |
| 项目骨架（50 文件） | src/ tests/ config/ scripts/ | ✅ |
| **SSH 执行抽象层骨架** | src/toolkit/core/executor.py | ✅（Sprint 1 实现） |
| **测试环境部署记录（Runbook）** | docs/10-测试环境部署记录.md | ✅ |
| **使用手册（命令详解+案例+开发测试规则）** | docs/11-使用手册.md | ✅ |
| README / CHANGELOG / Dockerfile / 配置模板 | 根目录 + config/ | ✅ |

## 4. 架构决策（全部锁定，可直接照此编码）
| ADR | 决策 | 影响 |
|---|---|---|
| ADR-01 | **管理机 SSH 远程控制恢复机** | 所有命令走 `core/executor.py`，禁止散落 subprocess |
| ADR-02 | ~~官方 tarball 装 MySQL~~ → **ADR-08 升级为 Docker** | 见 ADR-08/09 |
| ADR-03 | **SQLite 元数据库** | `data/toolkit.db`，表结构与 MySQL 兼容 |
| ADR-04 | **scp 拉取备份** | 从备份源机 scp 到恢复机临时目录 |
| ADR-05 | 单目标机**串行**恢复 | datadir 唯一，避免冲突；复用固定端口 13306 |
| ADR-06 | **接管 crontab 备份**（分阶段） | Sprint 1-2 只拉取+演练，Sprint 3 才触发备份 |
| ADR-07 | installer/notifier/verifier 抽象基类 | 扩展不改核心 |
| **ADR-08** | **技术选型与参考来源锁定**（见下方详述） | 备份架构 + 多版本方案 + 调研结论 |
| **ADR-09** | **Docker 恢复架构实现细节锁定**（见下方详述） | 容器策略 + 端口 + xtrabackup 执行位置 |
| **ADR-10** | **实现细节锁定**（见下方详述） | 验证规则 + 密码安全 + 配置格式 + 错误日志采集 |

### ADR-08 详述：技术选型与参考来源（2026-08-13 锁定）

> 完整调研过程见 [`docs/07-技术调研报告.md`](07-技术调研报告.md)

**背景**：立项后进行了两轮 GitHub 开源项目调研，评估可复用的实现，避免重复造轮子。

**决策**：

| 子决策 | 选定 | 备选（未采用） | 理由 |
|---|---|---|---|
| 备份模块架构标杆 | **pyxtrabackup**（`deviantony/xtrabackup-scripts`）分层架构 | pyxtra（工程不合格）、gox（语言不符） | 工程规范最高：列表参数防注入、异常体系、logging、LSN 增量追踪、10步流水线 |
| 备份命令 | **`xtrabackup`**（8.0 兼容） | innobackupex（pyxtrabackup 原用，已废弃） | innobackupex 在 XtraBackup 8.0 已移除 |
| 增量备份追踪 | **LSN**（读 `xtrabackup_checkpoints` 的 `to_lsn`） | 目录名追踪（pyxtra 方式，脆弱） | LSN 是官方推荐方式，可靠 |
| 多版本恢复方案 | **Docker 容器隔离** | dbdeployer 物理多实例、tarball 多实例 | Docker 更干净（每次全新环境）、易清理；借鉴 restore-drill |
| 恢复演练设计 | 声明式配置 + 验证 SQL + 审计报告 | mneme（Ansible，重） | 借鉴 restore-drill（`RamazanKara/restore-drill`） |
| 远程 NAS 备份 | **rsync 推送** | NFS 挂载（依赖运维） | 参考 pyxtra 的 rsync 用法，工具自管 |
| 压缩 | tar.gz（备份模块内置） | — | 参考 pyxtrabackup |
| 保留策略 | 按天数/份数自动清理 | — | 新增（参考项目都没有，自研） |

**⚠️ 重要修正（覆盖 ADR-02 部分）**：
原 ADR-02 定为"官方 tarball 装 MySQL"，调研后**多版本方案升级为 Docker 容器隔离**：
- 恢复时不再在恢复机物理安装多版本 MySQL，而是为每次恢复启动对应版本的 Docker 容器（如 `mysql:8.0.35`、`mysql:5.7.44`）
- 这**不影响 FP-01 的 tarball 安装逻辑**（Docker 镜像本身就是版本隔离），但实现方式从"解压 tarball 到 `/opt/mysql-versions/`"变为"拉取/启动对应版本 Docker 镜像"
- **前置依赖**：恢复机必须安装 Docker。FP-01 的职责从"装 MySQL"调整为"管理 MySQL Docker 容器（拉镜像、启停、挂载 datadir）"

**不直接 fork 这些项目的原因**：
1. pyxtrabackup 依赖过老（Python 3.4 / innobackupex / docopt），改造量 ≈ 重写量
2. restore-drill 是 Go 写的，与本项目 Python 栈不符
3. 本项目要对上百实例负责，自研代码可完全掌控

**借鉴方式**：参考设计思路和架构，不复用代码。涉及 Apache 2.0 项目（pyxtrabackup、restore-drill），若复用代码片段须在 NOTICE 声明。

### ADR-09 详述：Docker 恢复架构实现细节（2026-08-13 锁定）

> ADR-08 确定了"用 Docker 容器隔离多版本 MySQL"。ADR-09 锁定具体怎么用 Docker。

**核心原则**：宿主机跑 xtrabackup + datadir 挂载进容器 + host 网络 + 固定单端口 + 容器常驻。

**子决策**：

| 子决策 | 选定 | 理由 |
|---|---|---|
| xtrabackup 执行位置 | **宿主机**（非容器内） | 不用自建含 xtrabackup 的镜像，用官方 `mysql:8.0` 镜像即可，最简单 |
| datadir 位置 | **宿主机** `/data/drill/{version}/datadir/`，挂载进容器 | 宿主机可跑 copy-back；独立目录避免与宿主 MySQL 权限冲突 |
| 网络模式 | **host**（`--network host`） | 直接用宿主机端口，最简单 |
| 端口策略 | **固定单端口 13306**（串行复用） | ADR-05 串行恢复，同一时刻只有一个容器跑，端口不冲突 |
| 容器生命周期 | **常驻**（按版本复用，不随实例删除） | 上百实例恢复时省去反复 docker run/pull 开销；同版本连续恢复直接复用 |
| 容器内 MySQL uid | **999:999**（官方镜像 mysql 用户） | copy-back 后宿主机 datadir 必须 `chown 999:999`，代码自动处理 |

**常驻容器池设计**：
- 每个需要的 MySQL 版本对应一个常驻容器：`drill-mysql-{version}`（如 `drill-mysql-8035`）
- 所有常驻容器配置 `--port=13306`（串行不冲突）
- 容器启动时 datadir 挂载到 `/data/drill/{version}/datadir`
- 首次需要某版本时 `docker run -d` 创建并加入池；之后 `docker start/stop` 复用

**单实例恢复标准流程**（串行）：
```
1. 确认目标版本常驻容器存在（首次则 docker run 创建，已存在则复用）
2. docker stop {当前容器}          # 若有其他版本容器在跑，先停掉
3. docker start {目标版本容器}      # 启动目标版本容器（端口 13306）
4. docker stop {目标版本容器}       # 立即停（copy-back 需要 MySQL 不运行）
5. 清空 /data/drill/{version}/datadir/*
6. 宿主机：xtrabackup --prepare --target-dir={备份临时目录}
7. 宿主机：xtrabackup --copy-back --target-dir={备份临时目录} --datadir=/data/drill/{version}/datadir
8. 宿主机：chown -R 999:999 /data/drill/{version}/datadir
9. docker start {目标版本容器}      # 启动，加载恢复后的 datadir
10. 连 127.0.0.1:13306 执行验证 SQL（FP-04）
11. 验证通过 → 归档日志（FP-08）
12. 取下一个任务：同版本 → 回到步骤4；不同版本 → 回到步骤2
```

**前置依赖（恢复机 bootstrap 时安装）**：
1. Docker（必装）
2. xtrabackup（必装，宿主机跑备份恢复）
3. 创建 `/data/drill/` 目录

**⚠️ 关键实现约束**：
- 所有 docker 命令经 `core/executor.py` 执行（SSH 到恢复机跑）
- 串行保证（ADR-05）：工具必须确保同一时刻只有一个常驻容器在 running 状态
- copy-back 前必须 `docker stop`，否则 datadir 被锁导致恢复失败

### ADR-10 详述：实现细节锁定（2026-08-13）

> 锁定验证规则、密码安全、配置格式、错误日志采集等实现细节。

**子决策**：

#### 1. 实例清单格式：单文件 YAML
- 所有实例写在 `config/instances.yaml`，DBA 批量编辑
- 结构：`defaults`（全局默认）+ `instances`（实例列表，每实例可覆盖默认）

#### 2. 密码安全：环境变量方案
- 密码**绝不写进配置文件**，存管理机环境变量 `DRILL_MYSQL_PWD`
- 工具启动时从环境变量读取，运行时注入给 xtrabackup（经 `--defaults-file` 临时文件或 stdin，不进 ps/history）
- 符合 `04-安全设计.md` 要求；后续可无缝升级到 Vault（运行时注入环境变量）
- **前提**：上百实例统一用一个只读备份账号（DBA 惯例）

#### 3. 恢复验证规则：自动发现（核心创新）
不用为每个实例手写验证 SQL，工具自动发现业务库表验证：

```
自动验证流程：
1. 连接恢复出的实例 127.0.0.1:13306
2. 查所有库 → 过滤系统库（mysql, information_schema, performance_schema, sys, test）
3. 对剩余业务库，选 TABLES 数量最多的库作为验证库
4. 在验证库中，选 TABLE_ROWS > 0 的第一张表
5. SELECT COUNT(*) FROM {库}.{表} → 结果 > 0 则 PASS
```

**边界判定**：
| 情况 | 判定 |
|---|---|
| 过滤系统库后无业务库 | **PASS**（MySQL 能起来即说明 datadir 恢复成功，只是无业务数据） |
| COUNT(*) > 0 | PASS |
| COUNT(*) = 0 或查询失败 | FAIL |

#### 4. 验证失败/启动失败处理
- **验证 FAIL**：自动进重试队列（FP-05），重试 1 次仍失败则记录失败 + 附详细诊断（发现了哪些库、选了哪个库哪张表、COUNT 结果、报错）
- **容器启动失败**（docker start 失败）：**必须采集并保留日志**
  - `docker logs {容器}` 抓取容器错误输出
  - 检查挂载 datadir 下的 `{hostname}.err`（MySQL error log）
  - 两份日志归档到 `/归档目录/YYYY-MM-DD/{源IP}_{版本}/error/`
  - 标记该实例失败，附日志路径

#### 5. 配置文件 YAML 结构（草案）
```yaml
# config/instances.yaml
defaults:
  backup_user: backup_reader          # 统一只读备份账号
  # password 不写这里，来自环境变量 DRILL_MYSQL_PWD
  drill_port: 13306                   # 固定端口（ADR-09）
  docker_datadir_base: /data/drill    # 容器 datadir 挂载根
  verify:
    exclude_system_dbs: [mysql, information_schema, performance_schema, sys, test]

instances:
  - name: order-db-prod-01
    host: 10.0.1.10                   # 源实例 IP
    port: 3306
    mysql_version: "8.0.35"
    backup_source:                    # 备份在哪台机器
      host: 10.0.1.250
      path: /backups/order-db-prod-01/
    # 可选：覆盖默认验证规则（绝大多数实例用自动发现即可，无需配）
```

## 5. Sprint 1 完成总结（核心代码就绪，待联调测试机）

### 代码文件索引
| 模块 | 文件 | 功能 |
|---|---|---|
| 命令执行地基 | `src/toolkit/core/executor.py` | LocalExecutor + SSHExecutor(paramiko) + FakeExecutor(测试) |
| 配置加载 | `src/toolkit/core/config.py` | pydantic v2 + ${ENV} 插值 + 实例清单 |
| 日志 | `src/toolkit/core/logger.py` | RotatingFileHandler + 幂等 |
| 数据模型 | `src/toolkit/core/models.py` | SQLAlchemy 6表 ORM + TaskStatus 枚举 |
| 数据库 | `src/toolkit/core/db.py` | 引擎/会话管理，SQLite/MySQL 兼容 |
| 异常体系 | `src/toolkit/core/exceptions.py` | 7 类业务异常 |
| 版本管理 | `src/toolkit/installer/version_manager.py` | 版本排序 + 容器名/路径生成 |
| Docker容器 | `src/toolkit/installer/docker.py` | 常驻容器池（ensure/start/stop/clean/chown/logs） |
| 备份定位 | `src/toolkit/backup/locator.py` | SSH扫描源机 + 完整性校验 |
| xtrabackup | `src/toolkit/backup/xtrabackup.py` | prepare/copy-back 封装（经executor） |
| 验证器 | `src/toolkit/recovery/verifier.py` | ADR-10 自动发现（系统库过滤→表最多库→有数据表→COUNT>0） |
| 恢复任务 | `src/toolkit/recovery/task.py` | ADR-09 12步编排 + 错误日志采集 |
| CLI | `src/toolkit/cli.py` + `__main__.py` | mysql/backup/drill/config 4组命令 |

### 已验证可用的命令
```bash
# 配置检查
toolkit config check --config config/config.yaml --instances config/instances.yaml

# 环境体检（SSH/Docker/xtrabackup）
toolkit config doctor --config config/config.yaml --target <恢复机IP>

# 容器管理
toolkit mysql ensure --config config/config.yaml --version 8.0.35 --target <IP> --yes
toolkit mysql list --config config/config.yaml --target <IP>

# 备份扫描
toolkit backup scan --config config/config.yaml --instances config/instances.yaml --yes
toolkit backup list --config config/config.yaml

# 恢复演练（dry-run 默认安全）
toolkit drill run --config config/config.yaml --instances config/instances.yaml --target <IP> --all --yes
```

### 测试
```bash
pytest tests/unit/ -v   # 87 个测试全过
```

### 下一步：联调（需要测试机）
Sprint 1 代码完成，但**尚未在真实环境验证**。下一步需要：
1. 在测试机（`118.145.108.178`）上安装 Docker + xtrabackup
2. 设置环境变量 `DRILL_MYSQL_PWD` + `DRILL_SSH_KEY`
3. 放一个小的测试备份
4. 跑 `toolkit mysql ensure --version 8.0.35` 验证容器创建
5. 跑 `toolkit drill run --instance xxx --yes` 验证端到端恢复

### ✅ 联调验证通过（2026-08-14）

**测试机**：`192.168.1.15`（CentOS 7, root, 端口 22）
**SSH 密钥**：`~/.ssh/toolkit_drill_ed25519`（管理机 Windows 上）

**环境就绪**：
- ✅ Docker 24.0.1（已启动 + 开机自启 + 镜像加速配置）
- ✅ xtrabackup 8.0.35-31（epel 修复后安装成功，zstd 依赖从阿里云 rpm 装）
- ✅ mysql:8.0.35 镜像（591MB，从 docker.1ms.run 拉取）
- ✅ 目录：`/data/drill/` `/data/archive/` `/data/logs/` 已创建

**端到端验证结果**（手动走 ADR-09 12 步）：
```
1. 常驻容器 drill-mysql-8035 创建 ✅（--network host --port=13306）
2. datadir 清空 ✅
3. xtrabackup --prepare ✅（completed OK!）
4. xtrabackup --copy-back ✅（completed OK!）
5. chown 999:999 ✅
6. docker start drill-mysql-8035 ✅
7. 自动验证：
   - 业务库发现：orders（2张表，表最多）✅
   - COUNT(orders.order_info) = 100 → PASS ✅
```

**联调中发现的环境细节（重要，后续开发参考）**：
1. 📌 **MySQL 8.0 容器需加 `--skip-log-bin`**：否则 xtrabackup 备份时拷 binlog 会失败（`binlog.000003 not found`）。测试环境无需 binlog。
2. 📌 **MySQL 8.0 认证需加 `--default-authentication-plugin=mysql_native_password`**：否则 `caching_sha2_password` 导致命令行连接困难。
3. 📌 **xtrabackup 备份源 MySQL 需 datadir 挂载到宿主机**：xtrabackup 既要连 MySQL server（TCP），又要读 datadir 文件。源容器必须 `-v datadir:/var/lib/mysql`。
4. 📌 **`--network host` 模式下 MySQL 初始化需要 35-40 秒**：`mysqladmin ping` 走 socket 可能假就绪，实际 TCP 端口监听要更晚。代码里等待时间建议 ≥ 35 秒。
5. 📌 **测试备份路径**：`/data/drill/test-backups/test-instance-1`（78MB，含 orders.order_info 100条 + orders.customer 10条）
6. 📌 **恢复机磁盘**：44G 总量，16G 可用（注意监控）
7. 📌 **Docker Hub 不可达**：必须用镜像加速（`docker.1ms.run` 等），已在 `/etc/docker/daemon.json` 配置

**常驻容器当前状态**：`drill-mysql-8035` 运行中，端口 13306，datadir 已恢复 orders 库（COUNT=100 验证通过）。

### ✅ CLI 端到端验证通过（2026-08-14）

联调发现的 7 个问题已全部修复进代码，`toolkit drill run --yes` **一条命令全自动跑通**：

```bash
# 管理机（Windows）上执行
set DRILL_SSH_KEY=C:\Users\Administrator\.ssh\toolkit_drill_ed25519
set DRILL_MYSQL_PWD=Test@123456
python -m toolkit drill run --config config/config.yaml --instances config/instances.yaml --target 192.168.1.15 --instance test-instance-1 --yes
```

**结果**：✅ 成功，耗时 21 秒，COUNT(orders.order_info)=100 → PASS

**12 步全自动执行日志**：
```
步骤1:  确保容器 drill-mysql-8035 ✅
步骤2:  停止其他容器 ✅
步骤3-4: 启动并停止容器（wait_ready=False）✅
步骤5:  清空 datadir ✅
步骤6:  xtrabackup prepare（completed OK!）✅
步骤7:  xtrabackup copy-back（completed OK!）✅
步骤8:  chown 999:999 ✅
步骤9:  启动容器 + 等待就绪（3秒就绪）✅
步骤10: 自动验证 → orders.order_info COUNT=100 → PASS ✅
步骤11: 归档日志 ✅
```

**代码修复清单（联调 7 问题 → 代码适配）**：
| 修复项 | 文件 | 内容 |
|---|---|---|
| `--skip-log-bin` | `installer/docker.py` | docker run 加参数，避免 xtrabackup 拷 binlog 失败 |
| `--default-authentication-plugin=mysql_native_password` | `installer/docker.py` | 避免 caching_sha2 认证问题 |
| `wait_ready()` 方法 | `installer/docker.py` | start 后轮询 TCP 就绪，解决 ping 假就绪 |
| `start(wait_ready=)` 参数 | `installer/docker.py` | 步骤3-4 不等待，步骤9 等待 |
| tee → 重定向 | `backup/xtrabackup.py` | prepare/copy-back 用 `>log 2>&1` 替代 `tee`（exit code 准确） |
| completed OK 校验 | `backup/xtrabackup.py` | copy-back 也校验 completed OK! |
| 步骤3-4 不等待 | `recovery/task.py` | `start(wait_ready=False)` 避免浪费时间 |

**Sprint 1 至此彻底完成**：代码 + 测试（87过）+ CLI 端到端验证（21秒一键恢复）全部通过。工具已"真正可用"。

---

## Sprint 2 完成总结（2026-08-14，分支 sprint2）

### 新增能力
| 能力 | 说明 |
|---|---|
| **多实例编排** | `drill run --all` 一条命令演练所有 enabled 实例，按版本升序+IP升序排队 |
| **失败重试** | 失败自动进重试队列，主队列空后重试，最多 N 次（配置 drill.max_retry） |
| **断点续跑** | 任务状态实时持久化到 SQLite；崩溃后 `--resume <run_id>` 从未完成任务继续 |
| **批次管理** | drill_runs 批次记录（总数/成功/失败/重试/耗时），`drill status` 可查 |
| **备份自动登记** | 无 backup scan 登记时，自动用 instance.backup_source_path 降级登记 |

### 新增/重写文件
| 文件 | 内容 |
|---|---|
| `recovery/orchestrator.py` | 编排引擎：批次创建→定位备份→建队列→逐个执行→重试→聚合结果 |
| `recovery/queue.py` | 任务队列：版本/IP 排序、双队列（todo+retry）、状态机、持久化、load_unfinished |
| `cli.py` drill run | 接入编排器，新增 `--resume` 参数 + 结果明细输出 |
| `tests/unit/test_orchestrator.py` | 11 个测试：排序、重试、失败-重试-成功、失败-重试-最终失败、断点加载 |

### 端到端验证（2026-08-14 测试机 192.168.1.15）
```
$ toolkit drill run --target 192.168.1.15 --all --yes
批次 #1：2 实例 42 秒全部成功
  ✅ test-instance-1 → orders.order_info COUNT=100（21s）
  ✅ test-instance-2 → inventory.products COUNT=50（20s）
```
- 两个实例数据独立验证无串扰（orders=100 / inventory=50）
- 备份无登记时自动降级登记 ✅
- 批次/任务全部入库 ✅

### 测试
98 个单元测试全过（Sprint 1 的 87 + Sprint 2 新增 11）

### 断点续跑用法
```bash
# 正常跑（崩溃后任务状态已持久化）
toolkit drill run --target <IP> --all --yes
# 重启后续跑
toolkit drill run --target <IP> --resume <批次ID> --yes
```

> ⚠️ 所有模块依赖 `CommandExecutor` 抽象，**禁止直接 subprocess/paramiko**。
> 现有 stub（如 `backup/xtrabackup.py` 里的 `import subprocess`）需在 Sprint 1 改为注入 executor。

## 6. Sprint 排期（ADR-06 分阶段后）
| Sprint | 目标 | 验收 |
|---|---|---|
| Sprint 1 | 地基 + 单实例恢复跑通 | 一条命令恢复一个备份并验证 |
| Sprint 2 | 多实例编排（版本排队、重试、断点续跑） | 一次跑通 N 个实例 |
| Sprint 3 | 报告 + 企微 + 日志归档 + **备份接管**（替换 crontab） | 出报告、发通知、工具触发备份 |
| Sprint 4 | 稳定性、文档、Docker 交付 | 可交付其他 DBA |

## 7. 已知问题 / 坑
- xtrabackup 版本需与 MySQL 版本匹配，需维护版本矩阵（config 已配）
- MySQL 8.0 默认 `caching_sha2_password`，连接器需注意（PyMySQL 已支持）
- SSH 免密需提前打通：管理机→恢复机、管理机→备份源机
- 恢复机磁盘：每次恢复前清 datadir + 拉取备份，注意临时空间
- 上百实例全量演练耗时长，需断点续跑（Sprint 2）
- 🆕 **Docker 容器内 mysql 用户 uid=999**：copy-back 后宿主机 datadir 必须 `chown 999:999`（代码自动处理）
- 🆕 **copy-back 前必须 docker stop**：否则 datadir 被锁导致恢复失败
- 🆕 **串行保证**：同一时刻只能有一个常驻容器在 running（都用 13306 端口）
- 🆕 **恢复机前置依赖**：必须预装 Docker + xtrabackup + 创建 `/data/drill/`

## 8. 本地运行方式（骨架阶段）
```bash
# 安装依赖（含 paramiko）
pip install -r requirements.txt
# 跑现有单元测试（2 个用例，不依赖未实现代码）
pytest tests/unit -v
# Sprint 1 完成后：
# python -m toolkit drill run --target <recovery-host> --instance <name> --yes
```

## 9. ⛔ 不要做的事（给下一个 AI 的红线）
- ❌ 不要在元数据库存明文密码
- ❌ 不要让工具连接生产库执行写操作（只读账号）
- ❌ 不要在业务模块直接 subprocess/paramiko —— 一律走 `core/executor.py`
- ❌ 不要并发在同一台目标机恢复多个实例（datadir 冲突）
- ❌ 不要跳过 dry-run 和预检查（磁盘、白名单）
- ❌ 不要在本期做 Web UI 复杂功能、监控、HA、数据同步
- ❌ 不要默认并发；串行为准（ADR-05）
- ❌ 不要在 Sprint 1-2 做"触发备份"（ADR-06 分阶段，Sprint 3 才做）

## 10. 待办池（未来，本期不做）
- 多目标机并发恢复
- 增量备份恢复
- PDF 报告 / 邮件 / 钉钉通知
- Web UI 历史趋势、RBAC
- 日志全文检索

## 11. 给新 AI 会话的开场白模板
```
你是资深 Python 工程师，接手 MySQL 自动化恢复演练工具。
请先读 docs/99-交接文档.md（含全部锁定决策与红线），再读
docs/02-架构设计.md（第 7 节执行抽象层）和 docs/01-PRD.md。
本次任务：【Sprint 1 任务 X】。约束：所有远程操作经 core/executor.py，
禁止散落 subprocess。完成后更新本交接文档。
```
