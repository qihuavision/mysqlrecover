# 使用手册 · MySQL 自动化备份与恢复演练工具

> 版本：v0.4.0 | 适用：Sprint 1~4 全部功能
> 完整命令参考 + 配置说明 + 真实案例 + 开发测试规则

---

## 目录

1. [工具简介](#1-工具简介)
2. [架构一览](#2-架构一览)
3. [环境要求与安装](#3-环境要求与安装)
4. [配置文件详解](#4-配置文件详解)
5. [命令详解（含案例）](#5-命令详解)
6. [完整使用流程案例](#6-完整使用流程案例)
7. [开发测试规则（必读）](#7-开发测试规则)
8. [Docker 化运行](#8-docker-化运行)
9. [常见问题排查](#9-常见问题排查)

---

## 1. 工具简介

**解决什么问题**：上百个 MySQL 实例的 xtrabackup 备份，需要定期恢复演练验证可用性。手动流程：每台装 MySQL → 恢复 → 登录验证 → 循环上百次。

**本工具**：一条命令完成全部 —— 自动管理多版本 MySQL（Docker 容器池）→ 按版本排队恢复 → 自动发现业务库验证 → 失败重试 → 断点续跑。

```
一条命令：
toolkit drill run --target <恢复机IP> --all --yes
```

---

## 2. 架构一览

```
┌─────────────────────┐         ┌──────────────────────────────┐
│  管理机（跑工具）    │  SSH    │  恢复机（一台空服务器即可）    │
│  Windows / Linux    │ ──────▶ │                              │
│                     │         │  Docker                      │
│  · toolkit CLI      │         │   ├─ drill-mysql-8035 (8.0)  │
│  · SQLite 元数据    │         │   └─ drill-mysql-5744 (5.7)  │
│  · 备份源机配置     │         │  xtrabackup（宿主机）         │
└─────────────────────┘         │  /data/drill/ 数据目录       │
                                └──────────────────────────────┘
```

- **多版本方案**：每个 MySQL 版本一个常驻 Docker 容器（端口固定 13306，串行复用）
- **恢复方式**：宿主机跑 xtrabackup prepare + copy-back → datadir 挂载进容器
- **验证方式**：自动发现（过滤系统库 → 选表最多的库 → 有数据的表 → COUNT>0）

---

## 3. 环境要求与安装

### 管理机（跑工具的机器）

| 要求 | 说明 |
|---|---|
| Python | ≥ 3.10（开发环境 3.12 验证通过） |
| SSH 私钥 | 能免密登录恢复机 |

```bash
# 安装
git clone https://github.com/qihuavision/mysqlrecover.git
cd mysqlrecover
pip install -e .

# 验证
toolkit --version
toolkit --help
```

### 恢复机（恢复演练的目标机）

| 要求 | 说明 |
|---|---|
| OS | Linux（CentOS 7 验证通过） |
| Docker | ≥ 20.x（含镜像加速配置） |
| xtrabackup | 8.0（MySQL 8.x 备份）+ 2.4（如需 MySQL 5.7） |
| 磁盘 | 建议 ≥ 50GB（镜像 + 备份 + datadir） |
| 目录 | `/data/drill/` `/data/archive/` `/data/logs/` |

一键准备环境（恢复机上执行）：
```bash
bash scripts/bootstrap_recovery_host.sh   # 装 Docker + xtrabackup + 建目录
```

### 环境变量（管理机上设置）

```bash
# 必须：恢复机 SSH 私钥路径
export DRILL_SSH_KEY=~/.ssh/id_rsa        # Windows: set DRILL_SSH_KEY=C:\keys\id_rsa

# 必须：恢复出的 MySQL root 密码（原生产 root 密码，验证登录用）
export DRILL_MYSQL_PWD='your_password'

# 可选：企业微信通知 webhook（Sprint 3）
# export DRILL_WECOM_WEBHOOK='https://qyapi.weixin.qq.com/...'
```

---

## 4. 配置文件详解

### 4.1 全局配置 `config/config.yaml`

```yaml
database:
  url: sqlite:///data/toolkit.db        # 元数据库（可换 MySQL，见数据库设计文档）

docker:
  drill_port: 13306                     # 恢复容器端口（固定，串行复用）
  datadir_base: /data/drill             # datadir 挂载根目录
  supported_versions:                   # 版本白名单（不在列表的拒绝恢复）
    - "8.0.35"
    - "5.7.44"

xtrabackup:
  binary_path: /usr/bin/xtrabackup      # 恢复机上的 xtrabackup 路径

target:
  host: 192.168.1.15                    # 恢复机 IP
  ssh_user: root
  ssh_port: 22
  ssh_key_env: DRILL_SSH_KEY            # 私钥从哪个环境变量读
  tmp_backup_dir: /data/drill/tmp-backups

backup:
  source_host: 192.168.1.15             # 备份归档所在机器
  source_root: /data/backups            # 备份根目录

drill:
  max_retry: 1                          # 失败重试次数（Sprint 2）
  dry_run_default: true                 # 默认 dry-run（安全）

archive:
  root: /data/archive                   # 审计日志归档根目录

logging:
  level: INFO
  file: /data/logs/toolkit.log
```

### 4.2 实例清单 `config/instances.yaml`

```yaml
instances:
  - name: order-db-prod-01              # 实例名（唯一，人类可读）
    host: 10.0.0.11                     # 源实例 IP
    port: 3306
    mysql_version: "8.0.35"             # 决定用哪个常驻容器恢复
    backup_source_host: 10.0.0.200      # 备份归档所在机器
    backup_source_path: /data/backups/order-db-prod-01   # 备份目录
    enabled: true                       # 是否纳入 --all 演练

  - name: user-db-prod-01
    host: 10.0.0.12
    port: 3306
    mysql_version: "5.7.44"
    backup_source_host: 10.0.0.200
    backup_source_path: /data/backups/user-db-prod-01
    enabled: true

# 上百个实例按同样格式继续添加...
# ⚠️ 密码不写这里！统一走环境变量 DRILL_MYSQL_PWD
# ⚠️ 验证 SQL 无需配置！工具自动发现业务库表（ADR-10）
```

---

## 5. 命令详解

### 5.1 `toolkit config check` —— 配置检查

**用途**：验证配置文件语法、必填字段、环境变量是否就绪。**建议每次改配置后先跑这个**。

```bash
toolkit config check --config config/config.yaml --instances config/instances.yaml
```

输出示例：
```
=== 配置检查 ===
✅ 全局配置加载成功: config/config.yaml
✅ 实例清单加载成功: 3 个实例

=== 环境变量 ===
  DRILL_MYSQL_PWD: ✅ 已设置
  DRILL_SSH_KEY: ✅ /root/.ssh/id_rsa

=== 支持版本 ===
  ['5.7.44', '8.0.35']
```

---

### 5.2 `toolkit config doctor` —— 环境体检

**用途**：检查恢复机的 SSH 连通性、Docker、xtrabackup、目录。**首次部署或环境异常时用**。

```bash
toolkit config doctor --config config/config.yaml --target 192.168.1.15
```

输出示例：
```
=== 环境体检 ===
  SSH 192.168.1.15: ✅
  Docker: ✅ Docker version 24.0.1
  xtrabackup: ✅
  /data/drill: ✅
```

---

### 5.3 `toolkit mysql ensure` —— 确保常驻容器存在

**用途**：为某 MySQL 版本创建常驻恢复容器（幂等，已存在则跳过）。**通常无需手动跑，drill run 会自动调用**。

```bash
# 确保 8.0.35 版本容器存在
toolkit mysql ensure --config config/config.yaml --version 8.0.35 --target 192.168.1.15 --yes
```

行为：
1. 校验版本在白名单内
2. 检查容器 `drill-mysql-8035` 是否存在（存在则跳过）
3. 不存在则 `docker run` 创建（含联调验证的参数：`--skip-log-bin`、`mysql_native_password`）
4. 创建后立即停止（常驻 stopped 状态）
5. 登记到元数据库

---

### 5.4 `toolkit mysql list` —— 列出常驻容器

```bash
toolkit mysql list --config config/config.yaml --target 192.168.1.15
```

输出示例：
```
CONTAINER                      STATUS
drill-mysql-8035               stopped
```

---

### 5.5 `toolkit backup scan` —— 扫描登记备份

**用途**：SSH 到备份源机，扫描各实例的备份目录，登记最新可用备份（校验 `xtrabackup_checkpoints` 完整性）。

```bash
# 扫描所有实例
toolkit backup scan --config config/config.yaml --instances config/instances.yaml --yes

# 只扫某个实例
toolkit backup scan --config config/config.yaml --instances config/instances.yaml --instance order-db-prod-01 --yes
```

> 💡 不 scan 也能跑 drill：orchestrator 会自动降级用 `instances.yaml` 里的 `backup_source_path` 登记。scan 的价值是校验备份完整性 + 找最新时间戳的备份。

---

### 5.6 `toolkit backup list` —— 列出已登记备份

```bash
toolkit backup list --config config/config.yaml
```

---

### 5.7 `toolkit backup trigger` —— 触发备份（Sprint 3，替代 crontab）

**用途**：SSH 到实例所在机器执行 `xtrabackup --backup`，产物自动登记入库。这是“备份接管”的核心命令（ADR-06），配 crontab 定时跑即可完全替代旧的备份脚本。

```bash
# dry-run 看计划
toolkit backup trigger --config config/config.yaml --instances config/instances.yaml \
  --instance order-db-prod-01 --backup-root /data/backups

# 真正执行
toolkit backup trigger --config config/config.yaml --instances config/instances.yaml \
  --instance order-db-prod-01 --backup-root /data/backups --yes
```

**参数说明**：

| 参数 | 必填 | 说明 |
|---|---|---|
| `--instance NAME` | ✅ | 要备份的实例名（instances.yaml 里的） |
| `--backup-root` | 可选 | 备份落盘根目录，默认 /data/backups |
| `--yes` | 建议加 | 不加则 dry-run |

**执行流程**：
```
1. 磁盘空间预检（<10G 拒绝执行）
2. 创建备份目录 {backup_root}/{实例名}/{时间戳}/
3. xtrabackup --backup（密码经临时凭据文件 600 权限传递，用完即删，不进 ps/history）
4. 校验产物完整性（xtrabackup_checkpoints）
5. 自动登记到 backups 表
```

**配 crontab 定时备份**（替代旧方案）：
```bash
# 每天凌晨 2 点批量备份所有 enabled 实例（crontab -e）
0 2 * * * cd /path/to/mysqlrecover && DRILL_SSH_KEY=~/.ssh/id_rsa DRILL_MYSQL_PWD='xxx' toolkit backup trigger --all --yes >> logs/backup.log 2>&1
```

**100 个实例的完整自动化流程（人工零介入）**：
```bash
# ① 一次性配置（人工仅此一次）：
#    - instances.yaml 填 100 个实例
#    - 恢复机跑 bootstrap + 各版本 MySQL 镜像 + xb 镜像
#    - 管理机设 DRILL_SSH_KEY / DRILL_MYSQL_PWD 环境变量
#    - crontab 加下面两行：

# ② 每天定时批量备份（100 个全自动，失败实例重试后仍失败的列入汇总）
0 2 * * * toolkit backup trigger --all --yes >> logs/backup.log 2>&1

# ③ 每周日定时全量恢复演练（用最新备份，版本自动检测 + redo 分组并行 + 自动验证 + 报告/通知）
0 3 * * 0 toolkit drill run --target 192.168.1.15 --all --yes >> logs/drill.log 2>&1

# ④ 每周清理过期数据
0 4 * * 0 toolkit cleanup --target 192.168.1.15 --yes >> logs/cleanup.log 2>&1
```

**“点击一下恢复 100 个”的保证机制**：
- 版本自动检测：备份是什么版本，自动路由对应容器（配置填错也会纠正）
- redo 兼容分组：同代合并串行、跨代并行（5.7 ∥ 8.0 同时恢复）
- 版本铁闸：每实例验证前校验 MySQL 版本，杜绝连错实例的假成功
- 失败自动重试 1 次；崩溃后 `--resume <批次ID>` 断点续跑
- 全程无需人工；结果看报告 + 企业微信通知

> ⚠️ 容器化 MySQL 备份（如恢复容器 13306）：工具自动加 `--datadir` 指宿主机挂载路径。物理机 MySQL 无需处理。

---

### 5.8 `toolkit drill run` —— 恢复演练（🔥 核心）

**这是最常用的命令**。三种模式：

#### 模式 1：演练所有启用实例（日常主用）

```bash
# 先看执行计划（dry-run，默认安全）
toolkit drill run --config config/config.yaml --instances config/instances.yaml \
  --target 192.168.1.15 --all

# 确认无误后真跑
toolkit drill run --config config/config.yaml --instances config/instances.yaml \
  --target 192.168.1.15 --all --yes
```

#### 模式 2：演练单个实例（调试/补验）

```bash
toolkit drill run --config config/config.yaml --instances config/instances.yaml \
  --target 192.168.1.15 --instance order-db-prod-01 --yes
```

#### 模式 3：断点续跑（工具崩溃后恢复）

```bash
# 崩溃后查看批次 ID
toolkit drill status --config config/config.yaml

# 从批次 3 继续未完成任务
toolkit drill run --config config/config.yaml \
  --target 192.168.1.15 --resume 3 --yes
```

**参数说明**：

| 参数 | 必填 | 说明 |
|---|---|---|
| `--target` | ✅ | 恢复机 IP |
| `--all` | 二选一 | 演练所有 enabled 实例 |
| `--instance NAME` | 二选一 | 只演练指定实例 |
| `--yes` | 建议加 | 不加则 dry-run 只打印计划 |
| `--resume ID` | 可选 | 断点续跑的批次 ID |
| `--config` | 可选 | 配置路径（默认 config/config.yaml） |
| `--instances` | 可选 | 实例清单路径（默认 config/instances.yaml） |

**执行流程**（每个实例自动走 12 步）：
```
1-2. 确保常驻容器 + 停其他容器
3-5. 启停容器 + 清空 datadir
6-7. xtrabackup prepare + copy-back
8-9. chown 999:999 + 启动容器等就绪
10.   自动验证（过滤系统库→表最多库→有数据表→COUNT>0）
11.   审计日志归档
12.   失败自动重试（最多 max_retry 次）
```

**输出示例**：
```
待演练实例: 2 个 (dry_run=False)
批次 #1 创建
>>> [test-instance-1] 开始演练 (v8.0.35, 第1次)
  ... 12 步日志 ...
<<< [test-instance-1] ✅ 成功 (21s)

===== 演练完成 =====
批次: #1
总计: 2  成功: 2  失败: 0  重试: 0  跳过: 0
耗时: 42s

明细:
  ✅ test-instance-1 (v8.0.35) [SUCCESS] attempt=1 21s
  ✅ test-instance-2 (v8.0.35) [SUCCESS] attempt=1 20s
```

---

### 5.9 `toolkit drill status` —— 查看演练进度

```bash
toolkit drill status --config config/config.yaml
```

---

### 5.10 演练报告（Sprint 3，自动生成）

每次 `drill run --yes` 结束后自动生成 Markdown 报告（无需手动调用），位置：`report.output_dir`（默认 `/data/logs/reports/`）。

**报告内容**：
- 批次号、目标机、生成时间、执行人
- 汇总表：总数/成功/失败/重试/跳过/耗时/成功率
- 明细表：每个实例的状态、尝试次数、**验证库.表、COUNT 结果**、耗时、失败原因
- 失败实例跟进清单
- 验证方法说明（ADR-10 自动发现规则）

**报告示例**：
```markdown
# MySQL 恢复演练报告
- 批次：#3
- 目标主机：192.168.1.15

## 汇总
| 指标 | 数值 |
|---|---|
| 实例总数 | 2 |
| ✅ 成功 | 2 |
| 成功率 | 100.0% |

## 明细
| # | 实例 | 版本 | 状态 | 尝试 | 验证库.表 | COUNT | 耗时 |
|---|---|---|---|---|---|---|---|
| 1 | test-instance-1 | 8.0.35 | SUCCESS | 1 | orders.order_info | 100 | 20s |
| 2 | test-instance-2 | 8.0.35 | SUCCESS | 1 | inventory.products | 50 | 22s |
```

---

### 5.11 企业微信通知（Sprint 3，可选）

配置后每次演练结束自动推送摘要到群：

```bash
# 管理机设环境变量
export DRILL_WECOM_WEBHOOK='https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx'
```

```yaml
# config.yaml
notifiers:
  wecom:
    enabled: true
    webhook_env: DRILL_WECOM_WEBHOOK
    notify_on: always    # always=每次通知 / on_failure=仅失败时通知
```

通知内容：批次、成功/失败数、成功率、失败实例明细（最多5个）、报告路径。发送失败自动重试 3 次，仍失败只记日志（不阻塞演练）。

---

### 5.12 `toolkit cleanup` —— 清理超期数据（Sprint 4）

**用途**：清理旧任务记录、旧报告文件、远程临时备份，防止磁盘和数据库膨胀。

```bash
# dry-run 看会清理什么
toolkit cleanup --config config/config.yaml

# 真正执行（默认：任务180天/报告90天/临时备份7天）
toolkit cleanup --config config/config.yaml --yes

# 自定义保留期 + 清理远程临时备份
toolkit cleanup --config config/config.yaml \
  --target 192.168.1.15 \
  --tasks-days 180 --reports-days 90 --backups-days 7 --yes
```

**参数说明**：

| 参数 | 默认 | 说明 |
|---|---|---|
| `--tasks-days` | 180 | 数据库任务/日志保留天数（等保审计 6 个月） |
| `--reports-days` | 90 | 本地报告文件保留天数 |
| `--backups-days` | 7 | 恢复机临时备份保留天数 |
| `--target` | 无 | 恢复机 IP（不传则跳过远程清理） |

建议配 crontab 每周跑一次：
```bash
0 3 * * 0 cd /path/to/mysqlrecover && toolkit cleanup --target 192.168.1.15 --yes >> logs/cleanup.log 2>&1
```

---

## 6. 完整使用流程案例

### 场景：新接手 50 个 MySQL 实例，要做首次全量恢复演练

```bash
# ═══ 第 1 步：准备恢复机（一次性）═══
# SSH 到恢复机，装环境
ssh root@192.168.1.15
bash scripts/bootstrap_recovery_host.sh
exit

# ═══ 第 2 步：管理机配置（一次性）═══
cd mysqlrecover
cp config/config.example.yaml config/config.yaml
vim config/config.yaml       # 改 target.host 为恢复机 IP
cp config/instances.example.yaml config/instances.yaml
vim config/instances.yaml    # 添加 50 个实例信息

# 设环境变量
export DRILL_SSH_KEY=~/.ssh/id_rsa
export DRILL_MYSQL_PWD='生产root密码'

# ═══ 第 3 步：检查（每次改配置后）═══
toolkit config check --config config/config.yaml --instances config/instances.yaml
toolkit config doctor --config config/config.yaml --target 192.168.1.15

# ═══ 第 4 步：先 dry-run 看计划 ═══
toolkit drill run --config config/config.yaml --instances config/instances.yaml \
  --target 192.168.1.15 --all
# 检查输出的实例列表、版本排序是否正确

# ═══ 第 5 步：正式演练 ═══
toolkit drill run --config config/config.yaml --instances config/instances.yaml \
  --target 192.168.1.15 --all --yes
# 喝杯咖啡，50 个实例自动排队恢复+验证

# ═══ 第 6 步：查看结果 ═══
# 终端有汇总；审计日志在恢复机 /data/archive/YYYY-MM-DD/
# 元数据在管理机 SQLite：批次、每任务状态、验证结果可查

# ═══ 中途崩了？断点续跑 ═══
toolkit drill status --config config/config.yaml        # 找到批次 ID
toolkit drill run --target 192.168.1.15 --resume 5 --yes  # 续跑
```

---

## 7. 开发测试规则

> ⚠️ **每个 Sprint 开发完成后，必须做真实恢复测试。规则如下：**

### 7.1 测试铁律

1. **必须真实恢复**：单元测试（mock）≠ 真实验证。每个功能开发完成后，必须在恢复机上跑完整的 `drill run` 验证。
2. **备份自给自足**：用户不提供测试备份。开发者用脚本**模拟创建备份**（见 7.2）。
3. **模拟备份 = 真实备份**：脚本创建的 xtrabackup 备份与生产备份**格式完全一致**（同样的 xtrabackup 工具、同样的物理备份格式），恢复流程零区别。这是行业标准的测试方法。
4. **验证数据正确性**：恢复后必须确认恢复出的数据 = 备份时的数据（COUNT 对得上）。
5. **多实例必须测串扰**：≥2 个不同数据的备份连续恢复，确认各自验证的是各自的数据（不串）。

### 7.2 模拟备份创建方法

```bash
# 在恢复机上执行（工具自带脚本）
bash scripts/make_test_backup.sh       # 创建 test-instance-1（orders 库，100 条）
bash scripts/make_second_test_backup.sh # 创建 test-instance-2（inventory 库，3 表 50 条）
```

脚本做的事：
```
1. docker run 一个临时 MySQL 容器（模拟生产实例）
2. 建业务库表 + 插入已知数量的数据（如 100 条）
3. xtrabackup --backup 产出物理备份
4. 清理临时容器，保留备份目录
```

验证时确认 `COUNT(*)` 与插入数量一致（100 / 50），即证明数据完整恢复。

### 7.3 历次测试记录

| 日期 | Sprint | 测试内容 | 结果 |
|---|---|---|---|
| 2026-08-14 | Sprint 1 | 模拟备份恢复（orders 100条） | ✅ 21s COUNT=100 |
| 2026-08-14 | Sprint 1 | CLI 端到端（修复 7 问题后） | ✅ 21s COUNT=100 |
| 2026-08-14 | Sprint 2 | 2 实例编排恢复（orders+inventory） | ✅ 42s 各自数据无串扰 |

---

## 8. Docker 化运行（Sprint 4）

工具自身可容器化运行（管理机容器模式：容器里跑 toolkit，SSH 控制恢复机）。

### 构建

```bash
cd mysqlrecover
docker build -t mysqlrecover:latest .
```

### 运行（docker run）

```bash
docker run --rm \
  -v $(pwd)/config:/app/config:ro \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/logs:/app/logs \
  -v ~/.ssh:/app/ssh:ro \
  -e DRILL_MYSQL_PWD='密码' \
  mysqlrecover drill run --target 192.168.1.15 --all --yes
```

### 运行（docker compose，推荐）

```bash
# 项目根目录
export DRILL_MYSQL_PWD='密码'

# 体检
docker compose run --rm toolkit config doctor --target 192.168.1.15

# 演练
docker compose run --rm toolkit drill run --target 192.168.1.15 --all --yes

# 备份
docker compose run --rm toolkit backup trigger --instance xxx --yes

# 清理
docker compose run --rm toolkit cleanup --target 192.168.1.15 --yes
```

### 挂载点说明

| 挂载 | 内容 |
|---|---|
| `/app/config` | config.yaml + instances.yaml（只读） |
| `/app/data` | SQLite 元数据库（持久化） |
| `/app/logs` | 工具日志 + 演练报告 |
| `/app/ssh` | SSH 私钥（容器内自动设 DRILL_SSH_KEY=/app/ssh/id_rsa） |

> ⚠️ 注意：容器化的是**管理机**（跑工具），恢复机仍需按第 3 章准备 Docker + xtrabackup。

---

## 9. 常见问题排查

### Q1: `drill run` 报 "连接恢复实例失败"
- 检查 `DRILL_MYSQL_PWD` 是否设置（= 原生产 root 密码）
- 恢复出的 MySQL 是生产的字节级副本，密码也是生产那套

### Q2: xtrabackup prepare 报 "Neither found #innodb_redo"
- xtrabackup 版本与 MySQL 版本不匹配（如用 2.4 恢复 8.0 备份）
- 检查 `xtrabackup --version`，8.0 备份必须用 percona-xtrabackup-80

### Q3: docker pull 拉不到 mysql 镜像
- Docker Hub 国内不可达，配置镜像加速（`/etc/docker/daemon.json`）
- 见 `scripts/bootstrap_recovery_host.sh` 里的配置

### Q4: 容器启动后验证连不上（wait_ready 超时）
- MySQL 8.0 初始化需 35-40 秒，工具默认等 60 秒
- 超时则查：`docker logs drill-mysql-8035` + datadir 下 `*.err`

### Q5: 断点续跑怎么知道批次 ID？
- `toolkit drill status` 查看进行中的批次
- 或查 SQLite：`select id, status from drill_runs order by id desc limit 5;`

### Q6: 想加新 MySQL 版本怎么办？
1. `config.yaml` 的 `docker.supported_versions` 加版本号
2. 恢复机 `docker pull mysql:<版本>`
3. 确认对应 xtrabackup 版本已装（8.0↔xb80 / 5.7↔xb24）

### Q7: backup trigger 报 "password: not set"？
- **xtrabackup 8.0 不再读取 MYSQL_PWD 环境变量**（旧版本坑）
- 工具已改用 `--defaults-extra-file`（临时凭据文件），确认用 v0.3.0+
- 注意 `--defaults-extra-file` 必须是命令行**第一个参数**（手动执行时）

### Q8: 备份报 "Neither found #innodb_redo"？
- 容器化 MySQL 备份需显式 `--datadir` 指宿主机挂载路径
- 工具已自动处理（端口=drill_port 时自动加）；手动执行时加 `--datadir=/data/drill/{版本}/datadir`

### 7.3 历次测试记录

| 日期 | Sprint | 测试内容 | 结果 |
|---|---|---|---|
| 2026-08-14 | Sprint 1 | 模拟备份恢复（orders 100条） | ✅ 21s COUNT=100 |
| 2026-08-14 | Sprint 1 | CLI 端到端（修复 7 问题后） | ✅ 21s COUNT=100 |
| 2026-08-14 | Sprint 2 | 2 实例编排恢复（orders+inventory） | ✅ 42s 各自数据无串扰 |
| 2026-08-14 | Sprint 3 | 报告生成验证 | ✅ Markdown 报告完整 |
| 2026-08-14 | Sprint 3 | **完整闭环**：trigger 备份→自动选最新→恢复→验证→报告 | ✅ 25s COUNT=50 |
| 2026-08-14 | Sprint 5 | 版本自动检测+备份拉取（8.0双实例） | ✅ 56s 检测/拉取全工作 |
| 2026-08-14 | Sprint 5 | **多版本并行**：3实例2版本（8.0.35+8.0.36），含故意配错版本自动纠正 | ✅ **47s** 并行生效，billing=200/orders=100/inventory=50 |

---

## 8.5 多版本并行与版本自动检测（Sprint 5）

### 工作原理

**版本自动检测**：恢复前从备份文件的 `xtrabackup_info` 读 `server_version`，自动路由到对应版本容器。**配置里的 mysql_version 只是兜底**——填错也会被自动纠正：

```
实例 test-instance-3 版本自动检测: 8.0.36（覆盖手工配置 8.0.35）
```

**多版本并行**：每版本分配独立端口（版本升序→13306+N），同版本组内串行（datadir 唯一），跨版本线程并行：

```
端口分配: {'5.7.44': 13306, '8.0.35': 13307, '8.0.36': 13308}
版本分组: 3 组同时恢复（各占一个容器/端口/datadir）
```

上百实例多版本混合场景下，总耗时 ≈ 最慢版本的耗时（而非所有实例之和）。

### xtrabackup 版本矩阵（5.7 必读）

5.7 备份必须用 xtrabackup 2.4 恢复，但 xb24 与 xb80 包冲突（同二进制路径）。解决：独立目录安装 xb24，config 配置矩阵：

```yaml
xtrabackup:
  binary_path: /usr/bin/xtrabackup          # 默认（8.0）
  binary_matrix:                             # 版本→二进制
    "8.0": /usr/bin/xtrabackup
    "5.7": /opt/xb24/usr/bin/xtrabackup      # rpm2cpio 独立解包的 xb24
```

xb24 独立安装（恢复机上）：
```bash
yum install -y --downloadonly --downloaddir=/tmp percona-xtrabackup-24 || \
  wget https://repo.percona.com/.../percona-xtrabackup-24-2.4.29-1.el7.x86_64.rpm -O /tmp/xb24.rpm
mkdir -p /opt/xb24 && cd /opt/xb24
rpm2cpio /tmp/xb24.rpm | cpio -idv
# 二进制: /opt/xb24/usr/bin/xtrabackup
```

### 关闭并行（排障时）

```yaml
drill:
  parallel: false    # 改为纯串行（旧行为），方便定位问题
```
